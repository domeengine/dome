import "graphics" for Canvas, Color
import "dome" for Window
import "plugin" for Plugin

Plugin.load("synth")

class Game {
    static init() {

    }
    static update() {
    }
    static draw(dt) {
      Canvas.cls()
    }
}
